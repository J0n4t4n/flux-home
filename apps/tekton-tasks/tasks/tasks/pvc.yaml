---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: container
  namespace: tekton-tasks
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 40Gi
  storageClassName: longhorn
  volumeMode: Filesystem
